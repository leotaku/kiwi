#let query(_) = ()
#let query-label(_) = none
#let read-asset(_, anchor: ()) = asset("/dev/null", "")
#let input-of(_) = "virtual"
#let document-at(_) = document("virtual")[]
#let make-relative(path, _) = path
#let register-for-index(_) = {}
